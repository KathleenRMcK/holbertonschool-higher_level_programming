#!/usr/bin/node
if (isNaN(parseInt(process.argv[2]))) {
    console.log('Missing number of occurrences');
} else {
    const total = parseInt(process.argv[2]);
    for (let helper = 0; holder < total; holder++) {
        console.log('C is fun');
    }
}
